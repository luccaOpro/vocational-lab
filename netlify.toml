# Configuración de Netlify para Vocational Lab
# Le indica a Netlify cómo construir el sitio automáticamente.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
