---
/*
  Navegación superior — fija arriba mientras se hace scroll.
  En desktop: links visibles en línea.
  En mobile (≤720px): se esconde la fila de links y aparece el botón
  hamburguesa, que abre un panel lateral desde la derecha.
*/
---

<header class="nav">
  <div class="nav__inner container">
    <a href="#hero" class="nav__brand">
      <span class="nav__brand-mark">VL</span>
      <span class="nav__brand-name">Vocational Lab</span>
    </a>

    {/* Links visibles en desktop */}
    <nav class="nav__links" aria-label="Navegación principal">
      <a href="#caminos">Programas</a>
      <a href="#programa">Proceso</a>
      <a href="#equipo">Equipo</a>
      <a href="/aula/login" class="nav__aula">Aula</a>
      <a href="#contacto" class="nav__cta">Hablemos</a>
    </nav>

    {/* Acciones de la derecha en mobile: CTA Hablemos + hamburguesa.
        Se muestran sólo en el breakpoint mobile (≤720px). */}
    <div class="nav__mobile-actions">
      <a href="#contacto" class="nav__cta-mobile">Hablemos</a>

      <button
        class="nav__burger"
        type="button"
        aria-label="Abrir menú"
        aria-expanded="false"
        aria-controls="mobile-menu"
        data-menu-toggle
      >
        <span class="nav__burger-bar"></span>
        <span class="nav__burger-bar"></span>
        <span class="nav__burger-bar"></span>
      </button>
    </div>
  </div>
</header>

{/* Backdrop oscurecido detrás del panel */}
<div class="nav__backdrop" data-menu-backdrop hidden></div>

{/* Panel lateral del menú mobile */}
<aside
  id="mobile-menu"
  class="nav__panel"
  aria-label="Menú"
  aria-hidden="true"
>
  <div class="nav__panel-head">
    <span class="nav__panel-brand">Vocational Lab</span>
    <button
      class="nav__close"
      type="button"
      aria-label="Cerrar menú"
      data-menu-toggle
    >
      <svg viewBox="0 0 24 24" width="22" height="22" aria-hidden="true">
        <path d="M6 6 L18 18 M18 6 L6 18" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" fill="none" />
      </svg>
    </button>
  </div>

  <nav class="nav__panel-links" aria-label="Navegación mobile">
    <a href="#caminos" data-menu-link>Programas</a>
    <a href="#programa" data-menu-link>Proceso</a>
    <a href="#equipo" data-menu-link>Equipo</a>
    <a href="/aula/login" data-menu-link>Aula</a>
    <a href="#contacto" data-menu-link class="nav__panel-cta">Hablemos</a>
  </nav>
</aside>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: rgba(250, 247, 242, 0.92);
    backdrop-filter: saturate(140%) blur(8px);
    border-bottom: 1px solid var(--color-line);
  }

  .nav__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: 1rem;
    padding-bottom: 1rem;
  }

  .nav__brand {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    font-family: var(--font-serif);
    font-weight: 500;
  }

  .nav__brand-mark {
    display: inline-grid;
    place-items: center;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: var(--color-ink);
    color: var(--color-bg);
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0;
  }

  .nav__brand-name {
    font-size: 1.05rem;
  }

  /* Links del nav superior (desktop) */
  .nav__links {
    display: flex;
    align-items: center;
    gap: 1.8rem;
    font-size: 0.95rem;
  }

  .nav__links a {
    color: var(--color-ink-soft);
    transition: color 0.15s ease;
  }
  .nav__links a:hover { color: var(--color-ink); }

  .nav__cta {
    padding: 0.55rem 1rem;
    border-radius: var(--radius-sm);
    background: var(--color-accent);
    color: #fff !important;
  }
  .nav__cta:hover { background: var(--color-accent-dark); }

  /* Link "Aula" — botón secundario (borde) para diferenciarlo del CTA. */
  .nav__aula {
    padding: 0.5rem 0.9rem;
    border-radius: var(--radius-sm);
    border: 1px solid var(--color-ink);
    color: var(--color-ink) !important;
    transition: background 0.15s ease, color 0.15s ease;
  }
  .nav__aula:hover {
    background: var(--color-ink);
    color: var(--color-bg) !important;
  }

  /* ===== Acciones mobile (CTA + hamburguesa) ===== */
  /* En desktop el contenedor es invisible — los links inline se encargan. */
  .nav__mobile-actions {
    display: none;
    align-items: center;
    gap: 0.6rem;
  }

  /* CTA "Hablemos" — visible sólo en mobile, al lado del hamburguesa. */
  .nav__cta-mobile {
    display: inline-flex;
    align-items: center;
    padding: 0.5rem 0.9rem;
    border-radius: var(--radius-sm);
    background: var(--color-accent);
    color: #fff;
    font-size: 0.9rem;
    font-weight: 500;
    line-height: 1;
    white-space: nowrap;
    transition: background 0.15s ease;
  }
  .nav__cta-mobile:hover { background: var(--color-accent-dark); }

  /* ===== Botón hamburguesa ===== */
  .nav__burger {
    display: inline-flex;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 42px;
    height: 42px;
    padding: 0;
    background: transparent;
    border: 0;
    cursor: pointer;
    color: var(--color-ink);
  }
  .nav__burger-bar {
    display: block;
    width: 22px;
    height: 1.6px;
    margin: 0 auto;
    background: currentColor;
    border-radius: 2px;
    transition: transform 0.25s ease, opacity 0.25s ease;
  }
  /* Cuando el menú está abierto, el ícono ☰ se transforma en X */
  .nav__burger[aria-expanded="true"] .nav__burger-bar:nth-child(1) {
    transform: translateY(6.6px) rotate(45deg);
  }
  .nav__burger[aria-expanded="true"] .nav__burger-bar:nth-child(2) {
    opacity: 0;
  }
  .nav__burger[aria-expanded="true"] .nav__burger-bar:nth-child(3) {
    transform: translateY(-6.6px) rotate(-45deg);
  }

  /* ===== Backdrop oscurecido ===== */
  .nav__backdrop {
    position: fixed;
    inset: 0;
    background: rgba(19, 27, 61, 0.45); /* Azul noche translúcido */
    backdrop-filter: blur(2px);
    opacity: 0;
    transition: opacity 0.25s ease;
    z-index: 60;
  }
  .nav__backdrop[data-open="true"] {
    opacity: 1;
  }

  /* ===== Panel lateral derecho ===== */
  .nav__panel {
    position: fixed;
    top: 0;
    right: 0;
    height: 100dvh;
    width: min(82vw, 360px);
    background: var(--color-bg);
    border-left: 1px solid var(--color-line);
    box-shadow: -20px 0 60px rgba(19, 27, 61, 0.18);
    transform: translateX(100%);
    transition: transform 0.32s cubic-bezier(0.16, 1, 0.3, 1);
    z-index: 70;
    display: flex;
    flex-direction: column;
    overflow-y: auto;
  }
  .nav__panel[data-open="true"] {
    transform: translateX(0);
  }

  .nav__panel-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.1rem 1.5rem;
    border-bottom: 1px solid var(--color-line);
  }

  .nav__panel-brand {
    font-family: var(--font-serif);
    font-size: 1rem;
    color: var(--color-ink);
  }

  .nav__close {
    display: inline-grid;
    place-items: center;
    width: 38px;
    height: 38px;
    padding: 0;
    background: transparent;
    border: 0;
    cursor: pointer;
    color: var(--color-ink);
    border-radius: var(--radius-sm);
    transition: background 0.15s ease;
  }
  .nav__close:hover {
    background: var(--color-surface);
  }

  .nav__panel-links {
    display: flex;
    flex-direction: column;
    padding: 1.5rem 1.5rem 2rem;
    gap: 0.25rem;
  }

  .nav__panel-links a {
    font-family: var(--font-serif);
    font-size: 1.4rem;
    color: var(--color-ink);
    padding: 0.85rem 0;
    border-bottom: 1px solid var(--color-line);
    transition: color 0.15s ease, padding 0.15s ease;
  }
  .nav__panel-links a:hover {
    color: var(--color-accent);
    padding-left: 0.25rem;
  }

  .nav__panel-cta {
    margin-top: 1.2rem;
    border-bottom: 0 !important;
    background: var(--color-accent);
    color: #fff !important;
    text-align: center;
    border-radius: var(--radius-sm);
    padding: 0.95rem 1rem !important;
    font-family: var(--font-sans) !important;
    font-size: 1rem !important;
  }
  .nav__panel-cta:hover {
    background: var(--color-accent-dark);
    padding-left: 1rem !important;
  }

  /* ===== Breakpoint mobile ===== */
  @media (max-width: 720px) {
    /* Esconder los links inline del nav superior */
    .nav__links { display: none; }
    /* Mostrar el bloque mobile (CTA Hablemos + hamburguesa) */
    .nav__mobile-actions { display: inline-flex; }
    /* Reservamos el espacio del brand-name */
    .nav__brand-name { display: none; }
  }

  /* Respeta usuarios que prefieren menos movimiento */
  @media (prefers-reduced-motion: reduce) {
    .nav__panel,
    .nav__backdrop,
    .nav__burger-bar { transition: none; }
  }

  /* Bloqueo del scroll del body mientras el menú está abierto */
  :global(body.menu-open) {
    overflow: hidden;
  }
</style>

<script>
  // ============================================================
  // Lógica del menú mobile
  // ------------------------------------------------------------
  // - Abre/cierra al tocar cualquier elemento [data-menu-toggle]
  //   (botón hamburguesa o botón X).
  // - Cierra al tocar el backdrop oscurecido.
  // - Cierra al tocar un link de navegación (para ir a la sección).
  // - Cierra al apretar la tecla ESC.
  // - Bloquea el scroll del body mientras está abierto.
  // ============================================================

  const toggles = document.querySelectorAll<HTMLElement>('[data-menu-toggle]');
  const panel = document.getElementById('mobile-menu');
  const backdrop = document.querySelector<HTMLElement>('[data-menu-backdrop]');
  const burger = document.querySelector<HTMLElement>('.nav__burger');
  const links = document.querySelectorAll<HTMLAnchorElement>('[data-menu-link]');

  function setMenu(open: boolean) {
    if (!panel || !backdrop || !burger) return;

    panel.dataset.open = String(open);
    panel.setAttribute('aria-hidden', String(!open));

    if (open) {
      backdrop.hidden = false;
      // Forzar reflow para que la transición de opacidad arranque
      void backdrop.offsetWidth;
      backdrop.dataset.open = 'true';
    } else {
      backdrop.dataset.open = 'false';
      // Esconder el backdrop después de la transición
      setTimeout(() => { if (backdrop.dataset.open !== 'true') backdrop.hidden = true; }, 280);
    }

    burger.setAttribute('aria-expanded', String(open));
    burger.setAttribute('aria-label', open ? 'Cerrar menú' : 'Abrir menú');

    document.body.classList.toggle('menu-open', open);
  }

  toggles.forEach((el) => {
    el.addEventListener('click', () => {
      const isOpen = panel?.dataset.open === 'true';
      setMenu(!isOpen);
    });
  });

  backdrop?.addEventListener('click', () => setMenu(false));

  links.forEach((link) => {
    link.addEventListener('click', () => setMenu(false));
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && panel?.dataset.open === 'true') {
      setMenu(false);
    }
  });
</script>
